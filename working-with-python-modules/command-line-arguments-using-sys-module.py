import sys
# sys.argv of sys modules
print("sudhamsh") # python3 command-line-arguments-using-sys-module.py mars is a great pet(we can say this like command line arguments) - while passing this shit cli this won't affect the program, after passing the command line arguments how can we get command line scripts inside the script
print(sys.argv) # if we pass command line arguments, it's gonna print us a list in the console
print(sys.argv[0])